<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.geometry.Insets?>

<AnchorPane xmlns="http://javafx.com/javafx/11.0.1" 
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.gestaoprojetos.controller.LoginController"
            style="-fx-background-color: #2c3e50;">

    <VBox alignment="CENTER" spacing="20" AnchorPane.topAnchor="50" AnchorPane.bottomAnchor="50" 
          AnchorPane.leftAnchor="100" AnchorPane.rightAnchor="100">
        
        <!-- Cabeçalho -->
        <VBox alignment="CENTER" spacing="10">
            <Label text="🚀" style="-fx-font-size: 40px;"/>
            <Label text="Sistema de Gestão de Projetos" 
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: white;"/>
            <Label text="Faça login para continuar" 
                   style="-fx-text-fill: #bdc3c7; -fx-font-size: 14px;"/>
        </VBox>

        <!-- Formulário de Login -->
        <GridPane hgap="15" vgap="15" style="-fx-background-color: white; -fx-padding: 30; -fx-background-radius: 10;">
            <columnConstraints>
                <ColumnConstraints hgrow="SOMETIMES" prefWidth="100"/>
                <ColumnConstraints hgrow="SOMETIMES" prefWidth="250"/>
            </columnConstraints>
            
            <rowConstraints>
                <RowConstraints prefHeight="40"/>
                <RowConstraints prefHeight="40"/>
            </rowConstraints>

            <Label text="Login:" GridPane.columnIndex="0" GridPane.rowIndex="0" 
                   style="-fx-font-weight: bold; -fx-font-size: 14px;"/>
            <TextField fx:id="loginField" GridPane.columnIndex="1" GridPane.rowIndex="0" 
                       promptText="Digite seu login" style="-fx-font-size: 14px;"/>

            <Label text="Senha:" GridPane.columnIndex="0" GridPane.rowIndex="1" 
                   style="-fx-font-weight: bold; -fx-font-size: 14px;"/>
            <PasswordField fx:id="senhaField" GridPane.columnIndex="1" GridPane.rowIndex="1" 
                          promptText="Digite sua senha" style="-fx-font-size: 14px;"
                          onAction="#handleLogin"/>
        </GridPane>

        <!-- Botão de Login -->
        <Button text="Entrar" onAction="#handleLogin" 
                style="-fx-font-size: 16px; -fx-pref-width: 150px; -fx-pref-height: 40px; 
                       -fx-background-color: #3498db; -fx-text-fill: white; -fx-font-weight: bold;"/>

        <!-- Mensagem de Erro -->
        <Label fx:id="mensagemLabel" style="-fx-text-fill: #e74c3c; -fx-font-weight: bold; -fx-font-size: 12px;"/>

        <!-- Credenciais de Teste -->
        <VBox style="-fx-background-color: #34495e; -fx-padding: 15; -fx-background-radius: 10;">
            <Label text="🧪 Credenciais de Teste:" style="-fx-text-fill: white; -fx-font-weight: bold;"/>
            <Label text="Login: admin | Senha: admin123" style="-fx-text-fill: #bdc3c7;"/>
            <Label text="Login: gerente | Senha: gerente123" style="-fx-text-fill: #bdc3c7;"/>
            <Label text="Login: colaborador | Senha: colab123" style="-fx-text-fill: #bdc3c7;"/>
        </VBox>

    </VBox>

</AnchorPane>